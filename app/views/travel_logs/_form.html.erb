<%= simple_form_for @travel_log do |f| %>
  <% if @travel_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@travel_log.errors.count, "error") %> prohibited this travel_log from being saved:</h2>

      <ul>
      <% @travel_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :user_id, input_html: { class:"hide", value:current_user.id } %><p><%= current_user %></p>
  
  <%= f.label :avatar %><br>
  <%= image_tag(@travel_log.avatar.url(:small)) %>
  <%= f.file_field :avatar %>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
